{
  plugins = {
    fugitive = {
      enable = true;
    };

    gitblame = {
      enable = true;
    };

    gitsigns = {
      enable = true;
    };

    neogit = {
      enable = true;
    };
  };
}
